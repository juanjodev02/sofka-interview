import { StyleSheet } from "react-native";

import { defaultStyles } from "@/theme";

export const ButtonStyles = StyleSheet.create({
  primary: defaultStyles.button.primary,
  disabled: defaultStyles.button.primary.disabled,
});
