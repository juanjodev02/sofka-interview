import { StyleSheet } from "react-native";

import { defaultStyles } from "@/theme";

export const textStyles = StyleSheet.create({
  heading: defaultStyles.text.heading,
  title: defaultStyles.text.title,
  body: defaultStyles.text.body,
  caption: defaultStyles.text.caption,
  button: defaultStyles.text.button,
  header: defaultStyles.text.header,
});
